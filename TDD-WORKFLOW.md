# TDD Workflow Guide

このドキュメントは、本リポジトリの Web Components（主に DADS コンポーネント）を **TDD（Test-Driven Development）** で開発するためのガイドです。

## TDD の基本原則

### Red-Green-Refactor サイクル

```
🔴 RED      失敗するテストを書く
🟢 GREEN    最小限の実装でテストを通す
🔵 REFACTOR 挙動を変えずに設計を改善する
```

### テストファーストの考え方
- **不安をテストに変える**（「壊れていないか？」を自動化する）
- **仕様を具体化する**（テスト＝実行可能な仕様）
- **APIを小さく保つ**（テストしやすい設計＝過剰な責務分離を避ける）

## テストの配置（目安）

本リポジトリでは、対象に応じてテストファイルの置き場が分かれています。

- コンポーネント単体: `packages/components/**/**.test.ts`
- viewer/バインディング等: `src/**/**.test.ts`
- 横断的なテスト: `tests/**/**.test.ts`
- 共通セットアップ: `tests/setup.ts`
- 共通ヘルパ: `test/utils/test-helpers.ts`

> 補足: 既存互換のため `tests/setup.ts` からヘルパを再エクスポートしていますが、新規テストは可能なら `test/utils/test-helpers.ts` から直接 import してください。

## テストカテゴリ（目安）

- **Unit（中心）**: 属性/プロパティ、レンダリング、イベント、境界条件
- **Integration**: 複数要素の連携、ユーザーフロー、DOM更新の整合性
- **Accessibility**: ロール/ラベル/状態（ARIA）・キーボード操作・フォーカス
- **Performance（必要な場合のみ）**: ループ/大量要素での極端な劣化を検知

## 実践ワークフロー

### Phase 1: RED（テストを書く）
1. 期待する振る舞いを 1 つに絞る（「何ができればOKか」を1文で言える粒度）
2. まず失敗することを確認する（失敗理由が分かりやすい状態を作る）
3. 可能なら “壊れやすい境界” を先に書く（空入力/未指定/無効値など）

### Phase 2: GREEN（最小実装）
1. テストを通すための最小変更だけを入れる
2. 仕様外の挙動を増やしすぎない（後から収拾がつかなくなる）
3. DOM/属性/イベントは “最小・明示的” を優先する

### Phase 3: REFACTOR（設計改善）
1. テストが通ったまま、重複/複雑性を減らす
2. 変数設計（CSS var / トークン）や責務の整理を行う
3. 追加で “守りたい挙動” が見えたらテストを足す（戻り値/副作用など）

## レビュー時チェックリスト（簡易）

- [ ] 変更は最小限で、意図が追える
- [ ] テストが追加/更新され、失敗ケースをカバーしている
- [ ] DOM/属性の追加は最小で、既存APIを壊していない
- [ ] a11y（ラベル/ロール/フォーカス）を崩していない
- [ ] `npm run test:run` が通る（必要なら `npm run type-check` も）

## コマンド

```bash
# 監視（TDD）
npm run test:watch

# 単発実行
npm run test:run

# 型チェック
npm run type-check
```

