# チップタグ作例（Figma: 14607:1451）追加 Plan

## 目標
Figma の「宛先 + Chip Tag 2件」の作例を、チップタグのデモ内に追加し、Figma 画像と同等の見た目で再現する。

## 背景
- Figma `14607:1451` は「宛先」ラベルと Chip Tag 2件の並びを示す作例。
- 既存の `chipTag` デモに作例セクションがないため、追加が必要。

## スコープ
- やること：
  - `src/demos/showcase-components.ts` の `chipTag` デモに作例セクションを追加
  - Figma のテキスト/間隔/サイズに合わせたデモ用スタイルを付与
- やらないこと：
  - コンポーネント API の変更
  - CEM 更新やドキュメント大規模改修

## 前提 / 制約
- 作例は `chipTag` デモ内に追加する（他ページには増やさない）
- レイアウト値・テキストは Figma から取得して確定する
- Figma 取得は実装フェーズで `get_design_context` / `get_screenshot` を利用する

## 変更内容（案）
### データ / バックエンド
該当なし

### UI / UX
- `src/demos/showcase-components.ts`
  - `chipTag` デモ内に「作例」セクションを追加
  - 構成例:
    - 左に「宛先」ラベル
    - 右に `dads-chip-tag` を2つ横並び
  - Figma の spacing / font / alignment に合わせてデモ用スタイルを記述
  - Chip Tag 内のテキスト・アイコン有無は Figma の実データに合わせる

### その他（Docs/Marketing/Infra など）
該当なし

## 受入基準
- [ ] `chipTag` デモに作例セクションが表示される
- [ ] 「宛先」ラベル + Chip Tag 2件の並びが Figma と一致する
- [ ] Chip Tag 内のテキスト / アイコン有無が Figma と一致する

## リスク / エッジケース
- Figma のテキスト/アイコンが想定と異なる場合、デモ内容の再調整が必要
- 余白の差異が小さく目立ちにくいため、スクショ比較が必須

## 作業項目（Action items）
1. Figma `14607:1451` の `get_design_context` / `get_screenshot` を取得（完了条件: 余白・テキスト・アイコン有無が判明）
2. `chipTag` デモ内に作例セクションの HTML を追加（完了条件: ラベル + chip 2件が描画）
3. Figma に合わせてデモ用スタイルを調整（完了条件: spacing/size が一致）
4. Chip Tag 内テキスト・アイコンを Figma 値に合わせる（完了条件: 文言/アイコン一致）
5. 手動で viewer を確認（完了条件: 作例が Figma に近い見た目）
6. 必要があればデモ用補足コメント追加（完了条件: 最低限の意図説明がある）

## テスト計画
- 目視確認（viewer で chipTag デモを表示）

## オープンクエスチョン
該当なし
